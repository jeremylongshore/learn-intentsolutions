baseURL = "https://learn.intentsolutions.io/"
defaultContentLanguage = "en"
title = "Learn"

disableKinds = ["taxonomy", "term"]

[markup.goldmark.renderer]
  unsafe = true

[markup.highlight]
  style = "monokai"
  noClasses = false
  lineNos = false

[markup.tableOfContents]
  startLevel = 2
  endLevel = 4

[outputs]
  home = ["HTML"]
  section = ["HTML"]
  page = ["HTML"]

[params]
  description = "Jeremy Longshore's personal study notes — AWS, cloud, ops, and whatever else lands on the reading list."
  author = "Jeremy Longshore"

[languages.en]
  weight = 1
  languageName = "English"
  languageCode = "en-us"

[[menu.main]]
  name = "Home"
  url = "/"
  weight = 1

[[menu.main]]
  name = "AWS"
  url = "/aws/"
  weight = 10
